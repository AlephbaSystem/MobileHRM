<?xml version="1.0" encoding="utf-8" ?>
<popup:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:popup="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             x:Class="MobileHRM.Views.Popup.BusinessPopup"
             x:Name="page">
    <Frame Margin="20,100" >
        <ScrollView>
            <StackLayout x:Name="BusinessItems">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="Silver">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Tapped="Item_Tapped" CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>
                            <Grid ColumnDefinitions="*,4*">
                                <Label HorizontalTextAlignment="Center" Grid.Column="0" Text="{Binding id}" TextColor="Black" />
                                <Label HorizontalTextAlignment="Center" Grid.Column="1" Text="{Binding name}" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </StackLayout>
        </ScrollView>
    </Frame>
</popup:PopupPage>